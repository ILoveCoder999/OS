用户空间应用程序
    ↓
read()/write() 系统调用
    ↓
切换到内核模式
    ↓
内核执行实际的I/O操作
    ↓
返回结果到用户空间

为什么open,read,write它们是系统调用
用户空间到内核空间的切换,应用程序运行在用户空间,硬件设备（磁盘、终端等）由内核管理,需要切换到内核模式来执行实际的文件操作

系统调用的特点
特权操作：需要内核权限才能访问硬件,上下文切换：用户态↔内核态的切换有开销,错误处理：通过返回值判断成功失败,阻塞性：可能会阻塞进程直到I/O完成
